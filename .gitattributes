# Java-исходники — всегда текст для diff/blame/log -p, даже если в файле
# случайно оказался сырой байт (например NUL внутри строкового литерала,
# который следовало экранировать). Без этой строки git молча переключается
# на "Binary files differ", и ревью не видит содержимое файла вовсе — так
# уже дважды терялся из виду целый коммит с тестами.
*.java text
