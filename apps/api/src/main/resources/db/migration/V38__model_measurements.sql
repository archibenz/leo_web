-- Мерки ИЗДЕЛИЯ по размерам модели (решение владельца 24.09.2026, п. 15):
-- длина изделия, ширина по груди/талии/бёдрам, длина рукава, ширина плеч —
-- то, что меряется на самой вещи. JSON-массив строк {kind, values: {размер: см}}.
-- Массивом, а не объектом: черновик сливает вложенные объекты рекурсивно, а
-- массив заменяет целиком — снятая мерка не должна оставаться с прошлого раза.
-- NULL — мерок нет, таблицы на карточке нет. Прежняя таблица размеров была одна
-- на все вещи и выдумана, её сняли в #86.
--
-- Идёт ПОСЛЕ V37 (соцсети, #90): Flyway не примет V38 раньше V37.
ALTER TABLE product_models ADD COLUMN measurements JSONB;
